const currentDialLocation = 50
const dialTurns = []

for (let i = 0; i < dialTurns.length; i++) {

  const cell = dialTurns[i]
  if (cell.length > 0) {
    if (cell.startsWith('R')) {
      
    } elseif (cell.startsWith('L')) {
      
    }
  }
}
